# Структура проекта fingerprint-injector

```
fingerprint-injector/
│
├── 📄 go.mod                      # Go модуль и зависимости
├── 📄 go.sum                      # Checksums зависимостей
├── 📄 LICENSE                     # MIT лицензия
├── 📄 .gitignore                  # Git ignore правила
├── 📄 Makefile                    # Команды для сборки и тестирования
│
├── 📚 Документация
│   ├── 📄 README.md               # Основная документация
│   ├── 📄 QUICKSTART.md           # Быстрый старт
│   ├── 📄 ARCHITECTURE.md         # Архитектура проекта
│   ├── 📄 CONTRIBUTING.md         # Руководство для контрибьюторов
│   ├── 📄 CHANGELOG.md            # История изменений
│   └── 📄 PROJECT_STRUCTURE.md    # Этот файл
│
├── 🔧 Основной код
│   ├── 📄 fingerprint.go          # Структуры данных Fingerprint
│   ├── 📄 injector.go             # Логика инжектирования
│   ├── 📄 presets.go              # Готовые пресеты для разных ОС
│   └── 📄 utils.go                # Утилиты и генераторы
│
├── 🧪 Тесты
│   ├── 📄 fingerprint_test.go     # Тесты для fingerprint.go
│   └── 📄 injector_test.go        # Тесты для injector.go
│
├── 📁 examples/                   # Примеры использования
│   │
│   ├── 📁 basic/                  # Базовый пример
│   │   └── 📄 main.go             # Использование готового preset
│   │
│   ├── 📁 custom/                 # Кастомный fingerprint
│   │   └── 📄 main.go             # Создание своего fingerprint
│   │
│   ├── 📁 stealth/                # Stealth режим
│   │   └── 📄 main.go             # Максимальная защита от детекции
│   │
│   ├── 📁 with-proxy/             # С прокси
│   │   └── 📄 main.go             # Использование с прокси сервером
│   │
│   ├── 📁 multi-session/          # Множественные сессии
│   │   └── 📄 main.go             # Запуск нескольких браузеров
│   │
│   └── 📁 random/                 # Случайный fingerprint
│       └── 📄 main.go             # Генерация случайных параметров
│
└── 📁 .vscode/                    # Настройки VS Code
    ├── 📄 settings.json           # Настройки редактора
    ├── 📄 launch.json             # Конфигурации запуска
    └── 📄 extensions.json         # Рекомендуемые расширения
```

## Описание основных файлов

### Основной код

#### `fingerprint.go`

Содержит:

- Структуру `Fingerprint` со всеми параметрами
- Вложенные структуры: `Screen`, `WebGL`, `Canvas`, `WebRTC`, и т.д.
- Методы: `ToJSON()`, `NewDefaultFingerprint()`

#### `injector.go`

Содержит:

- Структуру `Injector` для инжектирования
- Методы:
  - `NewInjector()` - создание инжектора
  - `GetInjectionScript()` - генерация JS кода
  - `Inject()` - инжектирование скрипта
  - `SetUserAgentOverride()` - установка UA через CDP
  - `SetTimezoneOverride()` - установка timezone через CDP
  - `ApplyAll()` - применение всех настроек

#### `presets.go`

Содержит готовые функции для создания preset:

- `NewChrome119Windows11()` - Windows 11
- `NewChrome119MacOS()` - MacOS
- `NewChrome119Linux()` - Linux
- `NewChrome119Android()` - Android

#### `utils.go`

Содержит утилиты:

- `RandomUserAgent()` - генерация случайного UA
- `RandomizeFingerprint()` - добавление вариаций
- `RandomResolution()` - случайное разрешение
- `RandomTimezone()` - случайная временная зона
- `GenerateRandomFingerprint()` - полностью случайный fingerprint

### Тесты

#### `fingerprint_test.go`

Тесты для:

- Создания fingerprint
- Конвертации в JSON
- Валидации параметров
- Всех preset функций

#### `injector_test.go`

Тесты для:

- Создания injector
- Генерации JS кода
- Проверки наличия всех компонентов в скрипте

### Примеры

#### `examples/basic/`

Самый простой пример. Показывает:

- Создание контекста chromedp
- Использование готового preset
- Применение fingerprint
- Открытие сайта

#### `examples/custom/`

Кастомизация fingerprint. Показывает:

- Создание своего fingerprint
- Настройку всех параметров
- Проверку результатов

#### `examples/stealth/`

Максимальная защита. Показывает:

- Настройку chromedp для stealth
- Отключение WebRTC
- Увеличение canvas noise
- Проверку на anti-bot сайтах

#### `examples/with-proxy/`

Использование с прокси. Показывает:

- Настройку прокси в chromedp
- Синхронизацию fingerprint с локацией прокси
- Проверку IP адреса

#### `examples/multi-session/`

Множественные сессии. Показывает:

- Запуск нескольких браузеров параллельно
- Использование разных fingerprint
- Управление сессиями

#### `examples/random/`

Случайный fingerprint. Показывает:

- Генерацию случайного fingerprint
- Проверку на разных сайтах
- Изменение параметров между сессиями

## Зависимости

### Основные

- `github.com/chromedp/chromedp` - Управление Chrome через DevTools Protocol
- `github.com/chromedp/cdproto` - Chrome DevTools Protocol definitions

### Dev зависимости

- Go 1.21+ (рекомендуется)
- Chrome/Chromium браузер

## Сгенерированные файлы (в .gitignore)

```
chrome-data*/        # Директории с user data Chrome
coverage.out         # Coverage отчет
coverage.html        # HTML coverage отчет
bin/                 # Скомпилированные бинарники
```

## Workflow разработки

1. **Установка зависимостей**

   ```bash
   make deps
   ```

2. **Разработка**

   - Редактируйте файлы в корне проекта
   - Добавляйте тесты в `*_test.go`
   - Создавайте примеры в `examples/`

3. **Тестирование**

   ```bash
   make test
   ```

4. **Запуск примеров**

   ```bash
   make run-basic
   make run-stealth
   # и т.д.
   ```

5. **Очистка**
   ```bash
   make clean
   ```

## Точки расширения

### Добавление нового параметра fingerprint

1. Добавьте поле в `Fingerprint` struct в `fingerprint.go`
2. Обработайте в `GetInjectionScript()` в `injector.go`
3. Обновите все preset в `presets.go`
4. Добавьте тесты в `fingerprint_test.go`

### Добавление нового preset

1. Создайте функцию в `presets.go`
2. Следуйте паттерну `NewChromeXXXPlatform()`
3. Добавьте тест в `fingerprint_test.go`

### Добавление нового примера

1. Создайте директорию в `examples/`
2. Создайте `main.go` с полным примером
3. Добавьте команду в `Makefile`
4. Упомяните в `README.md`

## Лучшие практики

1. **Именование**

   - Используйте camelCase для приватных функций
   - Используйте PascalCase для публичных
   - Добавляйте комментарии к экспортируемым типам

2. **Тестирование**

   - Пишите тесты для новых функций
   - Стремитесь к >80% coverage
   - Используйте table-driven tests

3. **Документация**

   - Комментируйте сложную логику
   - Обновляйте README при добавлении функций
   - Добавляйте примеры использования

4. **Код-стиль**
   - Используйте `go fmt`
   - Следуйте [Effective Go](https://golang.org/doc/effective_go.html)
   - Проверяйте с помощью `golangci-lint` (если установлен)

## Размер проекта

| Компонент            | Количество | Размер          |
| -------------------- | ---------- | --------------- |
| Go файлов (основных) | 4          | ~1500 строк     |
| Go файлов (тесты)    | 2          | ~400 строк      |
| Примеров             | 6          | ~800 строк      |
| Документации         | 6          | ~2000 строк     |
| **Всего**            | **18**     | **~4700 строк** |

---

Обновлено: 2024-10-11
